
// Task Model: Define a Task model with 
// properties like id, title, description, 
// and completed.

export interface Task{
    id: number;
    title: string;
    description: string;
    completed: boolean;
}